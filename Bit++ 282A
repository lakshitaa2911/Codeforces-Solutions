#include<iostream>
#include<string>
using namespace std;
int main(){
	int n,X(0);
	cin>>n;
	while(n--){
		string a;
		cin>>a;
		if(a[1]=='+'){
			++X;
		}
		else{
			--X;
		}
		
	}
	
	cout<<X;
	return 0;
}
